import createFlexContainer from './helpers/createFlexContainer';

const defaultRowStyle = { flexDirection: 'row', justifyContent: 'space-between' };

export default createFlexContainer(defaultRowStyle, 'Row');
